# Database Configuration
POSTGRES_DB=productdomain
POSTGRES_USER=productuser
POSTGRES_PASSWORD=changeme-prod-password

# Redis Configuration
REDIS_PASSWORD=changeme-redis-password

# Spring Configuration
SPRING_PROFILES_ACTIVE=dev

# API Configuration
API_BASE_URL=http://localhost:8080
API_IMAGE=productdomain/api:latest

# UI Configuration
UI_IMAGE=productdomain/ui:latest
UI_PORT=3000

# Production-specific settings
# SPRING_PROFILES_ACTIVE=prod
# UI_PORT=80

# Optional: External services
# JAEGER_AGENT_HOST=jaeger
# JAEGER_AGENT_PORT=6831
# PROMETHEUS_ENDPOINT=http://prometheus:9090

# Optional: Email configuration
# SPRING_MAIL_HOST=smtp.example.com
# SPRING_MAIL_PORT=587
# SPRING_MAIL_USERNAME=noreply@example.com
# SPRING_MAIL_PASSWORD=changeme-email-password

# Optional: JWT configuration
# JWT_SECRET=changeme-jwt-secret
# JWT_EXPIRATION=86400000

# Optional: AWS configuration
# AWS_ACCESS_KEY_ID=changeme-aws-access-key
# AWS_SECRET_ACCESS_KEY=changeme-aws-secret-key
# AWS_REGION=us-east-1