# Default test configuration - uses H2 in-memory database
# This configuration is loaded when no specific profile is active

# Default to test profile if not specified
spring.profiles.default=test

# Logging
logging.level.org.springframework.test.context=INFO
logging.level.io.cucumber=INFO